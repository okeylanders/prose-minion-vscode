# Release Preparation: v2.2.4

**Date:** 2026-09-03
**Branch:** `release/v2.2.4`
**Previous version:** `2.2.3`
**New version:** `2.2.4` (patch)

## Change summary

- Added the live OpenRouter model `meta/muse-spark-1.3` to both
  `RECOMMENDED_MODELS` and `CATEGORY_MODELS`.
- Synchronized the Category Search manifest enum and added a shared-widget
  picker guard.
- Updated the model reference, consumer changelog, detailed changelog, README,
  and workspace package versions.
- Muse Spark 1.1 and 1.2 remain selectable; no defaults, saved selections,
  settings contracts, or message contracts change.

## Live provider evidence

- OpenRouter's `/api/v1/models` catalog on 2026-09-03 listed
  `meta/muse-spark-1.3` with a 1,048,576-token context window, multimodal
  text/image/video/file/audio input, and text output.
- No paid inference call was made.

## Validation

- Focused catalog architecture tests: 2 suites, 5 tests passed.
- Full Jest: 208 suites, 2,319 tests, and 2 snapshots passed.
- Full coverage suite passed.
- TypeScript: core, webview, and extension targets passed.
- Production build and bundle sentinel passed. The existing three webpack size
  warnings for the webview bundle remain.
- Lint: 0 errors and 957 established warnings.
- Production dependency audit: 0 vulnerabilities.
- Full dependency audit: one high Browserslist advisory, one moderate `qs`
  advisory, and one low `postcss-selector-parser` advisory. These are in
  build-tool dependency paths; the production-only audit is clean.

## Review

- No release-blocking correctness, architecture, or security issue found in the
  focused catalog/diff review.
- A transient lockfile edit incorrectly changed the `deep-equal` dependency
  version; it was detected by diff review and restored before staging.

## Modified files

- `packages/core/src/infrastructure/api/providers/OpenRouterModels.ts`
- `packages/core/src/__tests__/architecture/widgetModelsSync.test.ts`
- `apps/vscode-extension/package.json`
- `package.json`, `packages/core/package.json`, `package-lock.json`
- `apps/vscode-extension/README.md`, `apps/vscode-extension/CHANGELOG.md`
- `docs/RECOMMENDED_MODELS.md`, `docs/CHANGELOG-DETAILED.md`

## Remaining steps

1. Commit and push release preparation.
2. Build the VSIX and perform manual smoke testing.
3. After explicit manual-test approval, merge to `main`, tag, create the GitHub
   release, and publish to Marketplace only with separate explicit approval.
