# Release v2.2.3 preparation checkpoint

Prepared: 2026-09-02 08:23 CDT
Updated: 2026-09-02 10:55 CDT after the Gemini 3.8 Flash addition

## Release state

- Branch: `release/v2.2.3`
- Previous release: `v2.2.2`
- Target version: `2.2.3` (patch)
- Source baseline: `main` at `4cea113c` (`fix(workshop): prefer newer named checkpoints`)
- Scope since `v2.2.2`: the Workshop checkpoint conflict fix, release hardening,
  and the live-verified Gemini 3.8 Flash catalog addition.

## Behavior shipped

- Divergent `current.json` and named Workshop checkpoints sharing an identity
  are preserved instead of ordered by machine-local wall-clock timestamps.
- Expected per-copy metadata differences (derived summary metadata, named
  `savedAt`, machine-local activity time, and synthetic resume turns) do not
  break normal named autosave association. Only exact canonical resume markers
  receive that exemption; malformed turns fail closed as durable divergence.
- Any remaining durable-state difference detaches automatic named autosave and
  logs an explicit-open recovery instruction.
- Explicitly opening the named checkpoint promotes it to `current.json`; a
  second restart restores normal dual autosave association.
- Live OpenRouter evidence confirms `google/gemini-3.8-flash` with 1,048,576
  context tokens, up to 65,536 output tokens, multimodal input, reasoning,
  structured-output, and tool support. It is opt-in in Recommended Models and
  Category Search; the `:batch` variant and existing defaults remain unchanged.
- No schema migration, setting change, or message-contract change is included.

## Release review

- Light code review found the merged wall-clock comparator could overwrite
  genuinely newer portable work after a stale local resume write.
- Remediation replaced timestamp ordering with fail-closed durable-state
  comparison and added cross-machine, malformed-marker, and second-restart
  regression coverage.
- Final code and documentation re-reviews found no release blockers or warnings.

## Validation

- Live OpenRouter catalog and endpoint inventory: direct
  `google/gemini-3.8-flash` present with active Google and Google AI Studio
  endpoints; no paid inference call made.
- Category Search and shared Workshop/widget catalog guards: 5/5 passed.
- Focused persistence coordinator suite: 32/32 passed.
- Full Jest: 208 suites, 2,319 tests, 2 snapshots passed.
- Coverage: 82.44% statements, 71.78% branches, 82.41% functions, 82.73% lines.
- TypeScript: core, webview, and extension targets passed.
- Production build: passed; bundle sentinel passed; three established webpack
  size warnings remain.
- Lint: 0 errors, 957 established warnings.
- Production dependency audit: 0 vulnerabilities.
- Full dependency audit: 1 high Browserslist advisory and 1 low
  `postcss-selector-parser` advisory in development/build-only dependency paths;
  accepted for this patch with no runtime exposure.
- A clean dependency install was not reproduced because the configured
  CodeArtifact registry is unavailable in this environment. Validation used the
  existing dependency tree linked into the isolated worktree.
- Focused review of the catalog addition found no stale defaults, duplicate
  IDs, batch-route leakage, or catalog/manifest/documentation parity gaps.

## Files prepared

- `package.json`
- `package-lock.json`
- `packages/core/package.json`
- `apps/vscode-extension/package.json`
- `apps/vscode-extension/README.md`
- `apps/vscode-extension/CHANGELOG.md`
- `docs/CHANGELOG-DETAILED.md`
- `docs/RECOMMENDED_MODELS.md`
- `docs/adr/2026-07-14-workshop-session-persistence.md`
- `packages/core/src/infrastructure/api/providers/OpenRouterModels.ts`
- `packages/core/src/__tests__/architecture/widgetModelsSync.test.ts`
- `packages/core/src/application/services/workshop/WorkshopSessionPersistenceCoordinator.ts`
- `packages/core/src/__tests__/application/services/workshop/WorkshopSessionPersistenceCoordinator.test.ts`
- this checkpoint

## Remaining gates

1. Commit and push the late Gemini 3.8 Flash addition on `release/v2.2.3` after
   exact staged-diff checks.
2. Repackage and inspect `prose-minion-2.2.3.vsix`.
3. Stop for a quick manual model-picker/API smoke test of the rebuilt artifact.
4. Only after explicit approval: merge/tag/create the GitHub release.
5. Marketplace publication remains a separate explicit authorization.
