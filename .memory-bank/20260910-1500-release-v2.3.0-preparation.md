# Release preparation: v2.3.0

Date: 2026-09-10. Branch: `release/v2.3.0`.
Previous release: v2.2.4. Release base: `59c3f97d5eb8bca7352eddad05371e029995f213`.
Worktree: `/private/tmp/prose-minion-release-v2.3.0`.
Status: automated validation and VSIX inspection complete; awaiting manual-test approval. Do not merge yet.

## Scope and compatibility

17 commits since v2.2.4 before release preparation; merged PRs #114 and #115.
Minor version: context-file refresh, GPT-6 Astra pickers, 20,000-character writer
profiles, and Workshop context/excerpt UI improvements. Named checkpoints own
saved truth; read-only loading defers automatic context changes and resume notices;
cache retries cannot rewrite named checkpoints. Loading and stale status fixes included.
The multimodal-composer epic is planning, not shipped functionality.

Existing V1/V2 sessions remain readable without manual migration. File-backed
context attachments are intentionally read-only; text briefs remain editable.
A legacy rolling cache without clean provenance can yield one conservative recovery.
Git reverts replace a verified clean rolling cache, including autosaved author turns;
unsaved divergence remains recoverable. Startup mirror failure retains the loaded room;
explicit Open remains transactional. No timestamp comparison authorizes replacement.

## Light release review

CRITICAL resolved in `1213faa2`: manual refresh previously captured session-local
attachment IDs before asynchronous source reads without owning a session operation.
Open/New could replace the room, allowing delayed A content/provenance to overwrite B.
The session message owner now wraps manual scans in the existing coordinator queue,
rejects active response/wizard scans, and publishes scan busy state. Automatic scans
reuse their already-held operation without recursive queue acquisition.

Independent release reviewer reproduced the original failure using actual handler
and aggregate with deferred intake; after the fix, found no remaining blocker.
Twelve regression cases added: real-store Open/New isolation and named byte stability,
failure releasing ownership, routed Send/tool/guest/Open/New/second-refresh rejection,
active response/wizard refusal, and scan failure UI recovery. No paid provider calls.

WARNING deferred: accepted PR115 residuals (duplicate recovery on failed/retried
Open, extra named-file comparison, cache retry status repetition, minor docs/order);
PR114 large-session comparison optimization and composer-focus restoration.
Preserve post-write named rechecks: removing them reopens an intervening Git race.

## Dependency audit

Public npm registry audit: five packages (2 high, 2 moderate, 1 low), zero critical.
`npm audit` and `npm audit --audit-level=high` exit 1 for the listed findings.
`npm audit --omit=dev` reports zero vulnerabilities. Locked dependencies unchanged.
All flagged nodes are dev/build tooling; VSIX uses webpack bundles and
`vsce package --no-dependencies`, excluding node_modules and build tooling.
No advisory exploit was tested; tests/build exercise normal project-controlled inputs.
Dependency upgrades are deferred to a focused maintenance change.

| Package | Severity | Exposure and verification | Decision |
| --- | --- | --- | --- |
| browserslist 4.28.2 | High | Webpack/Babel/Autoprefixer compile targets and stats; trusted repo inputs, no runtime extension route. Production build exercises normal usage. No malicious-query test. GHSA-c83g-rgw3-j3cx; GHSA-73wf-gq98-2v4g. | Defer dev-tool update |
| js-yaml 3.15.1 / 4.3.1 | High | Istanbul/ESLint/textlint configuration, not extension runtime dependencies. Lint/coverage exercise trusted config, not hostile YAML merge exhaustion. GHSA-2883-xcg3-v3hh. | Defer dev-tool update |
| baseline-browser-mapping 2.10.24 | Moderate | Browserslist build target resolution; production build exercises normal inputs, no invalid-input exploit test. GHSA-w5vr-8v7q-w6rv. | Defer dev-tool update |
| qs 6.15.2 | Moderate | typed-rest-client in VSCE packaging/publishing toolchain. Not a runtime request parser. Packaging is normal-path smoke evidence; publishing transport and hostile options are untested. GHSA-x5fp-wj9c-mxmx; GHSA-4mjr-xmp4-gh2g. | Defer dev-tool update |
| postcss-selector-parser 6.1.2 / 7.1.0 | Low | Tailwind/PostCSS build pipeline processes repo CSS; successful build is normal-path evidence, no hostile AST test. GHSA-w9m9-85wc-3x92. | Defer dev-tool update |

Local raw audit evidence: `/private/tmp/prose-v230-audit-all.json`,
`prose-v230-audit-prod.json`, `prose-v230-audit-high.log`, and
`prose-v230-audit-paths.json`. Live public OpenRouter catalog confirmed
`openai/gpt-6-astra` (1,050,000 context) without a paid call.

## Validation

- Full coverage run: 210 suites, 2,409 tests, 2 snapshots passed.
- Coverage: 83.04% statements, 72.32% branches, 82.95% functions, 83.33% lines.
- Core/webview/extension typechecks pass.
- ESLint: 0 errors; 963 existing warnings (unchanged from pre-fix run).
- Production build passes; three existing webpack size/performance warnings.
- Bundle verification passes: all three Tailwind sentinels present.
- `git diff --check` passes.
- VS Code manual validation is still required; automated evidence is not UI proof.

## Release files and isolation

Versions: root, core, adapter package manifests and four lockfile metadata entries.
Docs: consumer changelog, README, detailed changelog, this checkpoint.
Race fix: coordinator, session/context handlers, slice composition, route harness,
context/session route tests, real-store integration tests, architecture ownership test,
and read-only-loading ADR amendment.

Primary worktree stays on main with unrelated untracked files preserved:
`.todo/features/feature-workshop-anthropic-prompt-cache/`, `Prose Minion.zip`,
`workshop-ai-service-conversation-ownership.md`.

## Remaining steps

1. Push release preparation and verify remote SHA parity.
2. Run `npm run package` and inspect VSIX version, contents, size, and SHA-256.
3. User installs the exact VSIX and tests activation/settings/offline features,
   Workshop automatic/manual loading, Git changes, first-interaction resume behavior,
   context refresh and session switching, and API-backed behavior with configured credentials.
4. Only after explicit manual-test confirmation: merge release, tag v2.3.0,
   create GitHub release attaching this VSIX, and clean the release branch.
5. Marketplace publication requires a separate explicit user request.

## Package verification

`npm run package` passed (all three typechecks, 210 suites / 2,409 tests / two
snapshots, production build and bundle sentinels, VSCE packaging).
Artifact: `/private/tmp/prose-minion-release-v2.3.0/apps/vscode-extension/prose-minion-2.3.0.vsix`.
Publisher: `OkeyLanders`; version 2.3.0; 194 archive entries; 11,804,899 bytes.
SHA-256: `86cd941005589cb82b614862c5c9649b758fc6db3875e5d5bf86c2049cd905d3`.
Packaged bundles exactly match the validated build; README and v2.3.0 changelog
section match source. VSCE rewrites two historical changelog links as expected.
No source, node_modules, maps, .git, .env, or key-file entries are present.
Release prep `33859b0d` was pushed with matching remote tracking SHA; this
checkpoint-only follow-up does not change packaged files.

Next action: install this VSIX for manual testing. Steps 1 and 2 above are complete.
