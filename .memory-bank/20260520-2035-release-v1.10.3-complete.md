# Release Complete: v1.10.3

**Date:** 2026-05-20
**GitHub Release:** https://github.com/okeylanders/prose-minion-vscode/releases/tag/v1.10.3
**Marketplace:** https://marketplace.visualstudio.com/items?itemName=OkeyLanders.prose-minion

---

## Release Summary

### Version

- Previous: `v1.10.2`
- New: `v1.10.3`
- Type: `PATCH`

### Key Changes

- Audited curated `RECOMMENDED_MODELS` and `CATEGORY_MODELS` against the live OpenRouter `/models` endpoint.
- Removed 5 stale IDs (`claude-3.7-sonnet`, `grok-4`, `grok-4-fast`, `grok-4.1-fast`, `grok-code-fast-1`).
- Added 26 new IDs to `RECOMMENDED_MODELS` (high + medium tiers + 3 codex) and 5 new IDs to `CATEGORY_MODELS` (non-thinking/predictable tier).
- Re-synced `package.json` `proseMinion.categoryModel` enum with `CATEGORY_MODELS` (30 entries, 1:1).

### Statistics

- Commits since last release: `2` (`915e587`, `c0b76d0`)
- PRs merged: `0` (catalog-only patch on `main`, no release branch per user request)
- Tests: `296` passing across 35 suites
- Build: clean (pre-existing webview size warnings only)

---

## Timeline

- Release prep committed: 2026-05-20 (commit `c0b76d0`)
- VSIX packaged: 2026-05-20 ~18:44 CDT
- Tag pushed: 2026-05-20
- Marketplace publish: 2026-05-20 ~20:35 CDT

---

## Notes

- Initial publish attempt failed with `Access Denied: The Personal Access Token used has expired.` — user minted a fresh PAT via https://aka.ms/vscodepat and re-ran `npx vsce login OkeyLanders`. Retry succeeded.
- Published the already-built artifact with `npx vsce publish --packagePath prose-minion-1.10.3.vsix` rather than re-packaging.
- Publish was deferred ~2 hours after tag/package (initial release session ended at Step 10; marketplace publish requested separately later that evening).
- `npm audit` posture unchanged from v1.10.2: 1 high (`fast-uri`, build-only), 1 moderate (`ws`, test-only), 4 low (test-only). None applicable to the shipped extension.
