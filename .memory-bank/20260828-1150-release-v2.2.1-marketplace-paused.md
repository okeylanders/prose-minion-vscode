# Release Paused: v2.2.1 Marketplace Publication

**Date:** 2026-08-28
**Status:** GitHub release complete; VS Code Marketplace publication blocked on publisher authentication.

## Completed

- Merged `release/v2.2.1` to `main` (`7bc1d3b9`) and pushed it.
- Created and pushed annotated tag `v2.2.1`; it is an ancestor of `origin/main`.
- Published the GitHub release with `prose-minion-2.2.1.vsix`:
  <https://github.com/okeylanders/prose-minion-vscode/releases/tag/v2.2.1>
- Verified the release artifact version is `2.2.1` and its SHA-256 is
  `6a14edba3413cd5c980e7eb376e1d785fd68e025b9199a3e631b7b81684f0d19`.

## Marketplace Blocker

The attempted command was:

```sh
npx vsce publish --packagePath /private/tmp/prose-minion-vsce-release-vnext/apps/vscode-extension/prose-minion-2.2.1.vsix
```

VSCE rejected its stored publisher credential with `TF400813`: the token's user is not authorized.
Refresh the Visual Studio Marketplace PAT for publisher `OkeyLanders`, then authenticate VSCE interactively:

```sh
cd /Users/okey.landers/GitHub/prose-minion-vscode/apps/vscode-extension
npx vsce login OkeyLanders
```

Afterward, rerun the publish command above and verify the Marketplace listing plus the downloaded VSIX manifest/hash.

## Release Notes

- `z-ai/glm-5.3-flash`, `deepseek/deepseek-v4-flash-0731`, and `qwen/qwen3.8-flash` are now curated category choices.
- `z-ai/glm-5.3` is also a curated recommended choice.
- The README retains the v2.2.0 Conversation Widgets details beneath the v2.2.1 catalog update.
- The `release/v2.2.1` branch is retained pending separate cleanup approval.
