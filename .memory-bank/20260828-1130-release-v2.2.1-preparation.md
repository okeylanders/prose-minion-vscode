# Release Preparation: v2.2.1

**Date:** 2026-08-28
**Branch:** `release/v2.2.1`
**Baseline:** v2.2.0
**Target:** v2.2.1 (patch)

## Release analysis

- One user-facing change since v2.2.0: the curated OpenRouter model catalog
  refresh in `21c091cf`.
- No breaking API, persistence, setting-default, or editor-content change.
- Existing saved model selections remain intact; Category Search continues to
  default to `openai/gpt-oss-120b:nitro`.

## Catalog evidence

- Compared the curated choices with the live 387-model OpenRouter response on
  2026-08-28.
- Added GLM 5.3, GLM 5.3 Flash, DeepSeek V4 Flash 0731, and Qwen3.8 Flash.
- Removed seven retired direct model IDs. All 97 Recommended and 42 Category
  direct IDs resolve live; Nitro is correctly retained as a routing suffix.

## Release files

- Updated the root, core, extension, and lockfile workspace versions to 2.2.1.
- Added the consumer changelog entry, detailed release notes, and Marketplace
  README highlight.

## Validation

- `npm test`: 208 suites / 2,310 tests / 2 snapshots passed.
- `npm run test:coverage`: 208 suites / 2,310 tests / 2 snapshots passed.
- `npm run build`: passed; webpack emitted its existing three webview-size
  advisories and the bundle sentinel passed.
- `npm run lint`: exit 0 with 957 existing warnings and zero errors.
- `npm audit --registry=https://registry.npmjs.org`: 0 vulnerabilities.
- `npm audit --audit-level=high --registry=https://registry.npmjs.org`: 0
  vulnerabilities.
- `npm run package`: passed after rerunning typecheck and the full Jest suite.
- VSIX: `apps/vscode-extension/prose-minion-2.2.1.vsix`, 194 files, 11.25 MB,
  SHA-256 `bd129188f8de308ca16070eac7e4ff7feb0628fc49f2563c9402968a42b78e3b`.

## Environment note

`npm ci` could not recreate the dependency tree: the configured CodeArtifact
token returned E401, and public npm lacks lockfile-resolved `p-try@2.2.1`.
Validation and packaging therefore used the existing dependency tree from the
unchanged main checkout, linked read-only into this temporary release worktree.

## Remaining gates

1. Commit and push release preparation.
2. Install and manually smoke-test the VSIX.
3. After explicit approval, merge to main, tag v2.2.1, and create the GitHub
   release.
4. Publish to the Marketplace only on explicit request.
