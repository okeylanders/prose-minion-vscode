# Docs TODO Audit Merge

**Date**: 2026-06-29 19:59
**Branch**: `docs/todo-audit-pr64`
**Status**: Ready to merge back to `main`

## What Changed

Completed the docs cleanup pass after PR 64:

- Archived released `.todo` feature work and refreshed active backlog guidance.
- Updated stale project docs and migration/archive breadcrumbs.
- Added the Ada Forge agent prompt and request-history-buttons feature spec.
- Left follow-up architecture and feature work tracked in `.todo` rather than scattered through stale planning files.

## Merge Plan

`docs/todo-audit-pr64` is ahead of `main` and not behind it, so the merge back
to `main` should be a fast-forward. After pushing `main`, the remote branch can
be deleted.

## Validation Notes

This pass is documentation-only. No test run was required for the memory-bank
entry or branch cleanup.
