# Release Preparation: v1.10.3

**Date:** 2026-05-20
**Branch:** main (no release branch per user request)
**Previous Version:** v1.10.2
**New Version:** v1.10.3
**Bump Type:** PATCH

---

## Changes Summary

### Features
- None (catalog-only patch)

### Fixes
- None

### Breaking Changes
- None — purely additive catalog update plus removal of 5 IDs that OpenRouter no longer serves (effectively dead entries already)

### Catalog Refresh
- Audited curated `RECOMMENDED_MODELS` and `CATEGORY_MODELS` against the live OpenRouter `/models` endpoint
- Removed 5 stale IDs: `claude-3.7-sonnet`, `grok-4`, `grok-4-fast`, `grok-4.1-fast`, `grok-code-fast-1`
- Added 26 new IDs to `RECOMMENDED_MODELS` (high + medium tiers + 3 codex)
- Added 5 new IDs to `CATEGORY_MODELS` (non-thinking/predictable tier)
- Re-synced `package.json` `proseMinion.categoryModel` enum with `CATEGORY_MODELS`

---

## Audit Results

### npm audit
- High: 1 (`fast-uri` — transitive of webpack/ajv, build-only, not shipped)
- Moderate: 1 (`ws` — transitive of `jest-environment-jsdom`, test-only)
- Low: 4 (test-environment only)
- **Applicable to runtime extension: None** — same posture as v1.10.2; no action taken

### Code Review (Explore subagent)
- Critical: None
- Warnings: None
- Reviewer verified zero duplicate IDs, well-formed object literals, intact exports, and bidirectional sync between `CATEGORY_MODELS` and the `package.json` enum

---

## Test Results
- Tests: 296/296 passing across 35 suites
- Build: clean (pre-existing webview size warnings only, unchanged from v1.10.2)
- Type-check: clean

---

## Files Modified
- `package.json` (version 1.10.2 → 1.10.3, `proseMinion.categoryModel` enum re-synced)
- `src/extension.ts` (version string banner)
- `src/infrastructure/api/providers/OpenRouterModels.ts` (catalog refresh — already committed in 915e587 before release prep started)
- `CHANGELOG.md`
- `docs/CHANGELOG-DETAILED.md`
- `README.md`

---

## Catalog Counts
- `RECOMMENDED_MODELS`: 53 → 74 (−5 stale, +26 new)
- `CATEGORY_MODELS`: 25 → 30 (+5 new)
- `package.json` enum: 30 entries (1:1 with `CATEGORY_MODELS`)

---

## Workflow Adaptation
User requested release-on-main (no release branch). Skipped Step 0.5 (branch creation) and adapted Step 10 to tag/release directly on main instead of merging from a release branch. All other steps run as standard.

---

## Next Steps
- [ ] Commit release prep to main
- [ ] Package vsix
- [ ] User manual testing
- [ ] Tag and create GitHub release
- [ ] Publish to marketplace (on request only)
